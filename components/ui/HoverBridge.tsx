// Hover-bridge is disabled for now (nothing imports this file
// anymore). Left as an inert placeholder — rather than deleting it
// outright — because Next.js's TypeScript check type-checks every
// .tsx file in the project whether or not anything imports it, and
// the previous version of this file failed that check on its own
// (`useRef<number>()` needs an explicit argument under the
// currently-installed @types/react). Safe to delete this file
// entirely if you'd rather not keep it around.
export {};

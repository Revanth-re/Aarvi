import PremiumScreen from "@/components/mobile/PremiumScreen";

export const metadata = {
  title: "Premium — Aarvi",
  description: "Unlock every episode, ad-free, with offline downloads.",
};

export default function PremiumPage() {
  return <PremiumScreen/>;
}
